name: Build Carlyle Arrow Watch APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Scarica il repository
        uses: actions/checkout@v4

      - name: Configura Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Configura Android SDK
        uses: android-actions/setup-android@v3

      - name: Installa componenti Android
        run: |
          sdkmanager "platforms;android-28" "build-tools;34.0.0"

      - name: Configura Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Estrai app orologio
        run: |
          rm -rf project
          mkdir project
          unzip -oq "CarlyleArrow_WearOS2_v2.zip" -d project

      - name: Compila APK orologio
        run: gradle --no-daemon -p project/carlyle_arrow_v2 :app:assembleDebug
      - name: Salva APK orologio
        uses: actions/upload-artifact@v4
        with:
          name: CarlyleArrow-watch-apk
          path: project/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
